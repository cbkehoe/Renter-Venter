<%- include('../partials/header') %>
<table id="list">
  <thead>
    <tr>
      <th>Apartment</th>
      <th>Address<br></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% rents.forEach(function(r) { %>
      <tr>
        <td><%= m.title %></td>
        <td><%= m.releaseYear %></td>
        <td><a href="/movies/<%= m._id %>">DETAILS</a></td>
      </tr>
      <% }); %>
    </tbody>
  </table>